name: GitHub Classroom Autograding
on: [push]
permissions:
  checks: write
  actions: read
  contents: read
jobs:
  autograding:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Check Files
        id: check-files
        uses: education/autograding-command-grader@v1
        continue-on-error: true
        with:
          test-name: 'Required Files'
          setup-command: 'chmod +x .github/scripts/check_assignment.sh'
          command: './.github/scripts/check_assignment.sh files'
          timeout: 1
          max-score: 25

      - name: Check HTML
        id: check-html
        uses: education/autograding-command-grader@v1
        continue-on-error: true
        with:
          test-name: 'HTML Structure'
          command: './.github/scripts/check_assignment.sh html'
          timeout: 1
          max-score: 25

      - name: Check CSS
        id: check-css
        uses: education/autograding-command-grader@v1
        continue-on-error: true
        with:
          test-name: 'CSS Mobile Optimizations'
          command: './.github/scripts/check_assignment.sh css'
          timeout: 1
          max-score: 25

      - name: Check Layout
        id: check-layout
        uses: education/autograding-command-grader@v1
        continue-on-error: true
        with:
          test-name: 'Layout & FPV Container'
          command: './.github/scripts/check_assignment.sh layout'
          timeout: 1
          max-score: 25

      - name: Autograding Reporter
        uses: education/autograding-grading-reporter@v1
        if: always()
        env:
          CHECK-FILES_RESULTS: "${{ steps.check-files.outputs.result }}"
          CHECK-HTML_RESULTS: "${{ steps.check-html.outputs.result }}"
          CHECK-CSS_RESULTS: "${{ steps.check-css.outputs.result }}"
          CHECK-LAYOUT_RESULTS: "${{ steps.check-layout.outputs.result }}"
        with:
          runners: check-files,check-html,check-css,check-layout
